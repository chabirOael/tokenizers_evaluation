{
  "permissions": {
    "allow": [
      "WebSearch",
      "Bash(apt list:*)",
      "Bash(dpkg -l)",
      "Read(//usr/lib/**)",
      "Bash(conda --version)",
      "Bash(uv --version)"
    ]
  }
}

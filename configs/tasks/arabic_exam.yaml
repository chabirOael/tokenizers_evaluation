# GENERATED by scripts/render_task_presets.py from ArabicExamTask.param_spec() — do not edit by hand,
# re-run the script after changing the spec (tests/test_task_param_specs.py pins the equality).
#
# A run NEVER reads this file. The pipeline takes a task's parameters from the experiment YAML's
# `sweep.tasks[].params` only; an absent key means the code default listed here. This file is the
# documentation of every parameter the `arabic_exam` task declares, with its default and type; the
# experiment console renders the same spec live as a typed card (`/api/schema` → task_params).
task:
  type: "arabic_exam"
  params:
    # HuggingFace dataset path of the benchmark; override only if it moves on the Hub. [str]
    dataset_name: "MBZUAI/ArabicMMLU"
    # One sub-config of the dataset (a topic / subject) instead of every config merged; null = all. [str | null]
    dataset_config: null
    # Where the HuggingFace datasets cache of this benchmark lives on disk. [path; advanced]
    cache_dir: "outputs/data_cache"
    # Token cap of prompt + continuation at scoring time; a row whose continuation the cap eats scores the sentinel for every choice (all_sentinel in the row dump). 1024 for 3-shot prompts under long-sequence tokenizers. [int; range 16–…]
    max_length: 512
    # Seed of the few-shot demonstration sampling (deterministic per row and sub-config). [int; range 0–…; advanced]
    seed: 42
    # Drop rows whose question / choices / context contain Latin letters before scoring (logs the count, warns on a wiped sub-config). [bool]
    clean_latin_rows: false
    # In-context demonstrations prepended to each prompt, sampled from the same sub-config with the row itself excluded. Absent = the pipeline injects evaluation.num_fewshot (3 in the reference configs); set it here to override for this task only. [int; range 0–…]
    num_fewshot: 0
